import 'package:flutter/material.dart';

class QuizModePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Quiz Mode'),
      ),
      body: Center(
        child: Text('Quiz Mode will start here.'),
      ),
    );
  }
}
